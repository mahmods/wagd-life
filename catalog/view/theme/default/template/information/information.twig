{{ header }}
<!-- Page Head -->
<div class="page-head">
    <div class="container">
        <h1>{{ heading_title }}</h1>
        <div class="breadcrumb">
            {% for breadcrumb in breadcrumbs %}
            <a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>
            {% endfor %}
        </div>
    </div>
</div>
<!-- // Page Head -->

<div id="information-information">
<!--  
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
-->

<!-- About US -->
        <div class="about-us">
            <div class="container">
                <h2 class="section-title2">{{ heading_title }}</h2>
                {{ description }}
            </div>
        </div>
        <!-- // About US -->
{{ content_top }}
{{ content_bottom }}
</div>



{{ footer }}