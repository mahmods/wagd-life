{{ header }}
<!-- Page Head -->
<div class="page-head">
    <div class="container">
        <h1>{{ heading_title }}</h1>
        <div class="breadcrumb">
            {% for breadcrumb in breadcrumbs %}
            <a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>
            {% endfor %}
        </div>
    </div>
</div>
<!-- // Page Head -->
<div id="common-success" class="thanks-page">
    <div class="content-box">
        <img src="catalog/view/theme/default/assets/img/success.png" alt="">
        <h3>{{ text_message }}</h3>
        <a href="{{ continue }}" class="btn primary">{{ button_continue }}</a>
    </div>
</div>
{{ footer }}